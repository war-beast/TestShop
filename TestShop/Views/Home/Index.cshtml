@model IEnumerable<TestShop.Models.Category>

@{

}

<!-- SECTION -->
<div class="section">
    <!-- container -->
    <div class="container">
        <!-- row -->
        <div class="row">
            <!-- ASIDE -->
            <div id="aside" class="col-md-3">
                <!-- aside Widget -->
                <div class="aside">
                    <h3 class="aside-title">Категории</h3>
                    <div class="checkbox-filter">

                        @foreach (var item in @Model)
                        {
                            <div class="input-checkbox">
                                <input type="checkbox" id="category-@item.Id" name="filter">
                                <label for="category-@item.Id">
                                    <span></span>
                                    @item.Name
                                    <small>(@item.Products.Count)</small>
                                </label>
                            </div>
                        }
                    </div>
                </div>
                <!-- /aside Widget -->
                <!-- aside Widget -->
                <div class="aside">
                    <h3 class="aside-title">Цена</h3>
                    <div class="price-filter">
                        <div id="price-slider"></div>
                        <div class="input-number price-min">
                            <input id="price-min" type="number">
                            <span class="qty-up">+</span>
                            <span class="qty-down">-</span>
                        </div>
                        <span>-</span>
                        <div class="input-number price-max">
                            <input id="price-max" type="number">
                            <span class="qty-up">+</span>
                            <span class="qty-down">-</span>
                        </div>
                    </div>
                </div>
                <!-- /aside Widget -->
                <div class="aside">
                    <button id="filter" class="primary-btn order-submit">Применить фильтр</button>
                </div>
            </div>
            <!-- /ASIDE -->
            <!-- STORE -->
            <div id="store" class="col-md-9">
                <!-- store top filter -->
                <div class="store-filter clearfix">
                    <div class="store-sort">
                        <label>
                            Сотрировать по:
                            <select class="input-select" id="sortType">
                                @{ Html.RenderAction("SortingList", "Home"); }
                            </select>
                        </label>
                    </div>
                </div>
                <!-- /store top filter -->
                <!-- store products -->
                <div class="row" id="productList">
                    @{ Html.RenderAction("SortedProducts", "Home"); }
                </div>
                <!-- /store products -->
            </div>
            <!-- /STORE -->
        </div>
        <!-- /row -->
    </div>
    <!-- /container -->
</div>
<!-- /SECTION -->

@section scripts{
    <script>
        $(document).ready(function () {
            $("#filter").click(function () {
                var category = function () { };
                var catList = $(".checkbox-filter input[name='filter']");
                var selected = new Array();
                $.each(catList, function () {
                    var cat = new category();
                    var input = $(this);
                    cat.Id = $(this).attr("id").replace("category-", "");
                    selected.push(cat);
                });

                var filter = {
                    Categories: selected,
                    MinPrice: $("#price-min").val(),
                    MaxPrice: $("#price-max").val(),
                    Sort: $("#sortType").val()
                }

                ReloadList(filter);
            });

            function ReloadList(filter) {
                $.ajax({
                    async: true,
                    type: 'POST',
                    dataType: 'json',
                    contentType: "application/json;charset=utf-8",
                    cache: false,
                    url: '/Home/SortedProducts',
                    data: JSON.stringify(filter)
                }).done(function (data) {
                    $("#productList").html(data.responseText);
                    }).fail(function (data) {
                        if (data.status === 200) {
                            $("#productList").html(data.responseText);
                        }
                });
            }
        });
    </script>
}